# Калькулятор стоимости комбайна + модификации НОМЕР 1

Это уже не простая Python-программа, которая помогает посчитать итоговую стоимость сельхозтехники, но еще и целый сайт (без вкусный, но я не веб дизайнер да и не хочется)
---

- Считает стоимость комбайна + выбранных модификаций
- Учитывает НДС (20%)
- Показывает рекомендации: если ты выбрал жатку — предложит бункер
- Сохраняет историю расчётов в `history.json`
- Сначала спрашивает данные пользователя (ФИО, компания и т.д.)
  
- Имеет REST API который я реализовал в test.py для наглядности ( не знаю как он поведет себя в гитхабе, но если перекачать весь проект себе на локальный компьютер и запустит то должно воркать, если нет , то - бонуска )

---

# Что внутри

```bash
PROJECT/
    static
    ├── style.css    # я его не использовал, просто для вида  
    templates
    ├── style.css    # собственно наш html сайт ( не судите строго, главное что работает ) 
├── app.py             # Запуск сайта
├── calculator.py      # Подсчёт стоимости
├── data_storage.py    # Работа с файлом истории
├── user.py            # Получение инфы от пользователя
├── modif.py           # Все модификации и рекомендации
├── history.json       # Хранит историю (если он пустой — создастся сам)
├── test.py            # Реализация REST API ?  
README.md          # Этот самый файл
